Run tests and help fix failures:
1. cd src && uv run pytest -v --tb=short
2. Read failing test files and source files being tested
3. Explain each failure and propose fixes
4. Ask if I want them applied